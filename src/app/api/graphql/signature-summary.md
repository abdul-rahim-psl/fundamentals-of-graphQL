```markdown
# Signature Summary

(fieldName): (
parent, // Result from previous resolver
args, // Arguments from the GraphQL operation
context, // Shared object for the request
info // Query execution details
) => { ... }
```
